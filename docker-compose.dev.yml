services:
  webapp: 
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: fast-api-web
    restart: always
    volumes:
      - ./src:/usr/src/app
    ports:
      - 9000:8000